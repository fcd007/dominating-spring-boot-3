spring:
  application:
    name: service

server:
  port: 8080
  servlet:
    context-path: /
#    context-path: /${spring.application.name}

logging:
  level:
    root: INFO

# D / T / A / P
database:
  url: localhost:3306
  username: mysql
  password: 1234
